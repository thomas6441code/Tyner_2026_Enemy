# Entity-Relationship Diagram

Built directly from `yner_main/database/migrations/*.php` — every table, column, and
foreign key below exists in the schema as shown (Spatie's `permissions`/`role_has_permissions`/
`model_has_permissions` pivot tables are omitted for readability; only `roles` and the
`model_has_roles` link to `users` are shown).

```mermaid
erDiagram
    USERS ||--o| EMPLOYEES : "linked to (nullable)"
    USERS ||--o{ MODEL_HAS_ROLES : "has"
    ROLES ||--o{ MODEL_HAS_ROLES : "assigned via"
    DEPARTMENTS ||--o{ EMPLOYEES : "has"
    WORK_SCHEDULES ||--o{ EMPLOYEES : "assigned to"
    EMPLOYEES ||--o{ DEVICE_ENROLLMENTS : "enrolled via"
    BIOMETRIC_DEVICES ||--o{ DEVICE_ENROLLMENTS : "enrolls"
    EMPLOYEES ||--o{ ATTENDANCE_RECORDS : "has"
    EMPLOYEES ||--o{ PERMISSION_REQUESTS : "submits"
    USERS ||--o{ PERMISSION_REQUESTS : "reviews (nullable)"
    PERMISSION_REQUESTS |o--o{ ATTENDANCE_RECORDS : "syncs onto (nullable)"
    EMPLOYEES ||--o{ AI_ANOMALIES : "flagged for"
    EMPLOYEES ||--o{ AI_PREDICTIONS : "scored for"
    DEPARTMENTS ||--o{ REPORT_SUMMARIES : "scoped to (nullable)"
    USERS ||--o{ REPORT_SUMMARIES : "generated by (nullable)"
    USERS ||--o{ AUDIT_LOGS : "performed by (nullable)"

    USERS {
        bigint id PK
        string name
        string email UK
        string password
        timestamp email_verified_at
    }

    ROLES {
        bigint id PK
        string name "Admin | HR Officer | Employee"
        string guard_name
    }

    MODEL_HAS_ROLES {
        bigint role_id FK
        string model_type
        bigint model_id FK
    }

    DEPARTMENTS {
        bigint id PK
        string name UK
        text description
    }

    WORK_SCHEDULES {
        bigint id PK
        string name UK
        time start_time
        time end_time
        smallint grace_period_minutes "default 15"
    }

    EMPLOYEES {
        bigint id PK
        bigint user_id FK "nullable, unique"
        bigint department_id FK "nullable"
        bigint work_schedule_id FK "nullable"
        string employee_code UK
        string first_name
        string last_name
        string phone "nullable"
        date hire_date "nullable"
        enum status "active | inactive"
    }

    BIOMETRIC_DEVICES {
        bigint id PK
        string name
        enum type "stub | zkteco | hikvision"
        string serial UK
        string host "nullable"
        int port "nullable"
        string username "nullable"
        text password "nullable"
        enum status "active | inactive"
    }

    DEVICE_ENROLLMENTS {
        bigint id PK
        bigint biometric_device_id FK
        bigint employee_id FK
        string device_user_id "unique per device"
    }

    RAW_ATTENDANCE_LOGS {
        bigint id PK
        string device_user_id
        datetime punched_at
        string device_serial
        json raw_payload "nullable"
        timestamp processed_at "nullable"
    }

    ATTENDANCE_RECORDS {
        bigint id PK
        bigint employee_id FK
        date work_date
        string status "present|late|absent|official_leave|sick_leave|permission_approved|field_duty"
        datetime first_in "nullable"
        datetime last_out "nullable"
        int worked_minutes "nullable"
        int late_minutes "default 0"
        int early_leave_minutes "default 0"
        boolean is_manual "default false"
        text remarks "nullable"
        bigint permission_request_id FK "nullable, unique per employee+work_date"
    }

    PERMISSION_REQUESTS {
        bigint id PK
        bigint employee_id FK
        string type "official_leave|sick_leave|permission|field_duty"
        date start_date
        date end_date
        text reason
        string attachment_path "nullable"
        string status "pending|approved|rejected|cancelled"
        bigint reviewed_by FK "nullable, users"
        text review_note "nullable"
        timestamp reviewed_at "nullable"
    }

    AUDIT_LOGS {
        bigint id PK
        bigint user_id FK "nullable"
        string auditable_type
        bigint auditable_id
        string action
        json old_values "nullable"
        json new_values "nullable"
        text note "nullable"
    }

    NOTIFICATIONS {
        uuid id PK
        string notifiable_type
        bigint notifiable_id
        string type
        text data
        timestamp read_at "nullable"
    }

    AI_ANOMALIES {
        bigint id PK
        bigint employee_id FK
        date work_date
        string method "isolation_forest | zscore"
        float score
        text explanation
        json features "nullable"
    }

    AI_PREDICTIONS {
        bigint id PK
        bigint employee_id FK "unique"
        float risk_score
        string risk_level "low | medium | high"
        json top_factors "nullable"
        string model_version "nullable"
        datetime computed_at
    }

    REPORT_SUMMARIES {
        bigint id PK
        date period_month "unique with department_id"
        bigint department_id FK "nullable, null = org-wide"
        text narrative
        json highlights "nullable"
        json recommendations "nullable"
        json stats "nullable, exact payload sent to Claude"
        string model "nullable"
        boolean fallback "default false"
        bigint generated_by FK "nullable, users"
    }
```

## Notable constraints

- `raw_attendance_logs` — unique on `(device_serial, device_user_id, punched_at)` so re-polling
  a device is idempotent (dedupe at ingestion).
- `attendance_records` — unique on `(employee_id, work_date)`; one row per employee per day.
- `ai_anomalies` — unique on `(employee_id, work_date, method)` so the nightly scoring job can
  upsert without duplicating flags.
- `ai_predictions` — unique on `employee_id`; only the latest risk score per employee is kept.
- `report_summaries` — unique on `(period_month, department_id)`, which is exactly the cache
  key the "Generate AI summary" action checks before calling Claude again.
