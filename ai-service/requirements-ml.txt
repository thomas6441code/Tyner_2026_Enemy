# ML / data — installed in Phase 7 when models are built.
# Run: pip install -r requirements-ml.txt
# Pins target Python 3.14 (the project's interpreter); these are the first releases with
# prebuilt 3.14 wheels — older pins fall back to a source build that fails without a compiler.
-r requirements.txt
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
joblib==1.5.3
